{
  "name": "sun-moon-transit-predictor",
  "version": "0.1.0",
  "description": "Predicts and detects aircraft transits across the sun and moon disc from a fixed observer location.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "NODE_OPTIONS=\"--experimental-sqlite --no-warnings=ExperimentalWarning\" vitest run",
    "test:watch": "NODE_OPTIONS=\"--experimental-sqlite --no-warnings=ExperimentalWarning\" vitest"
  },
  "dependencies": {
    "astronomy-engine": "^2.1.19"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
